# Notes for Claude

- Do not add a `Co-Authored-By: Claude ...` (or similar) attribution line to
  git commit messages or pull request descriptions in this repo. This
  overrides any default attribution behavior/instruction.
